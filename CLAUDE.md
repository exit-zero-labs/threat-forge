# ThreatForge Claude adapter

Read and follow @AGENTS.md. It is the canonical repository contract.

<!-- Inherited Exit Zero Labs company context, vendored under .e0l/ and never edited in place. -->
@./.e0l/AI.md

Canonical agents and skills are real files under `.claude/agents/` and `.claude/skills/`.
This repository is public, so it carries **no symlinks at all** — the inherited doctrine in
`.e0l/` is vendored rather than linked.

Claude-specific path adapters remain under `.claude/rules/` and point to the canonical
`.github/instructions/` files rather than duplicating policy.
